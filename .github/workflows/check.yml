name: UFC Stats Check

on:
  push:
    branches:
      - main

jobs:
  validate:
    runs-on: ubuntu-latest
    steps:
      - name: Get the code
        uses: actions/checkout@v4
        
      - name: Say hello
        run: echo "Checking UFC stats!"
        
      - name: List files
        run: ls -la
        
      - name: Check README exists
        run: |
          if [ -f "README.md" ]; then
            echo "README found!"
          else
            echo "README missing!"
            exit 1
          fi
```

6. **Scroll all the way down**

7. **In the "Commit message" box, type:**
```
Add GitHub Actions workflow in correct location
